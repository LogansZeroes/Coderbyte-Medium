function ThreeFiveMultiples(num) { 

  var arr = [];
  
  for ( var i = 0; i < num; i++ ) {
    if ( i % 3 == 0 || i % 5 == 0 ) {
      arr.push(i);
    }
  }

  return eval(arr.join('+'));       
  }
