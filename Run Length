function RunLength(str) { 
  
  var arr = [];
  var count = 1;
  
  for ( var i = 0; i < str.length; i++) {
    
    if (str[i] !== str[i+1]) {
      arr.push(count + str[i]);
      count = 1;
    }
    else count ++;
  }

  // code goes here  
  return arr.join(''); 
         
}
